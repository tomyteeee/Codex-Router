{
  "name": "codex-router-patcher",
  "private": true,
  "version": "0.1.0",
  "description": "Build tooling for the independent Codex Router macOS app patch",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tomyteeee/Codex-Router.git"
  },
  "homepage": "https://github.com/tomyteeee/Codex-Router#readme",
  "bugs": {
    "url": "https://github.com/tomyteeee/Codex-Router/issues"
  },
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "npm run check:go && npm run check:js && npm run check:python && npm run check:shell",
    "check:go": "go test ./... && go vet ./...",
    "check:js": "node --check ui/account-menu.js && node --check ui/thread-subscription.js && node --check ui/ui-test-bridge.cjs",
    "check:python": "python3 -m py_compile scripts/patch_app.py scripts/check_release.py",
    "check:shell": "bash -n install.sh",
    "release:check": "python3 scripts/check_release.py"
  },
  "devDependencies": {
    "@electron/asar": "4.2.1"
  }
}
